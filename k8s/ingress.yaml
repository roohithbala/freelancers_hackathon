apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: freelancers-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx-proxy-service
            port:
              number: 80
